# Railway Configuration for the NestJS API Server (Default)
[build]
builder = "NIXPACKS"
buildCommand = "npm run build -w shared && npm run build -w server"

[deploy]
startCommand = "npm run start -w server"
restartPolicyType = "ON_FAILURE"
